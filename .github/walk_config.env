# Params for .github/workflows/survivorship_shards.yml.
# On the walk branch, editing this file triggers a run; the periodic sims_full/
# checkpoint auto-commits never touch it, so they don't re-launch the matrix.
# These are the full-run defaults; workflow_dispatch inputs override per-run.
NUM_SHARDS=8
LIMIT=
DO_COLLECT=true
