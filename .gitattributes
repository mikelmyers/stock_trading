*.pkl filter=lfs diff=lfs merge=lfs -text
data/eodhd.zip filter=lfs diff=lfs merge=lfs -text
# Checkpoint files are regular blobs — too many small files for LFS verify to
# handle in this proxy environment, and they need to survive container resets.
training/cache/sims_full/*.pkl -filter -diff -merge -text
